run_script(assignment_number)		runs the assignment with default values
run_script(assignment_number, save)	as above but the figures will be saved automatically and closed afterwards

AssignmentX(name_of_file)		runs the Assignment with default values. Only Assignment2 needs datatyp
					specification "mm.jpg", "simple.png"